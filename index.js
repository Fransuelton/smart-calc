function clicou () {
    var n1 = Number(window.prompt('Digite um número: '))
    var n2 = Number(window.prompt('Digite outro número: '))
    var r = n1 + n2
    document.write(`O resultado da soma entre ${n1} e ${n2} é igual a: <strong>${r}</strong>`)
}
